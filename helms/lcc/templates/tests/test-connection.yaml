apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lcc.fullname" . }}-test-connection"
  labels:
    {{- include "lcc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lcc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
